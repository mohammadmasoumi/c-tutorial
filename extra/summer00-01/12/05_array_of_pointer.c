#include <stdio.h>
#include <string.h>

const int MAX = 10;

int main() {
    // char* myStudent[MAX];
    char myStudent[MAX][MAX];

    for (int i = 0; i < MAX; i++) {
        // scanf("%s", &s);
        gets(myStudent[i]);
        // strcpy(, gets());
    }


    return 0;
}