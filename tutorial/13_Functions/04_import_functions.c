#include "stdio.h"
#include "helper.h"

/*

#inlcude <package> | first the mingw packages then local files
#include "package" | first local files then mingw packages

*/

int main() {

    verySimpleSum2(1, 2);
    // printf("%d\n", verySimpleSum(1, 2));

    return 0;
}